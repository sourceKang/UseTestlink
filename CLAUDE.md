@AGENTS.md

先讀 `.agents/skills/testlink-agent/SKILL.md`，依任務載入所需流程。
Claude 接入與離線健檢見 `docs/claude-setup.md`。
匯入預覽可用 `qa_read_preview_artifact` 分頁檢查 items、warnings、ignored；
必須審閱完整內容並取得使用者明確確認，不能只根據摘要或 digest 自行寫入。
