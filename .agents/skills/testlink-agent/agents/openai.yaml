interface:
  display_name: "TestLink Agent"
  short_description: "Safely maintain TestLink QA workflows."
  default_prompt: "Use $testlink-agent to preview, verify, and audit this TestLink QA operation safely."

policy:
  allow_implicit_invocation: true
